name: Discover FGIS Export Inspections Format
on:
  workflow_dispatch:

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Examine CSV format
        run: python commodity-markets-deploy_2/scripts/discover_fgis.py
